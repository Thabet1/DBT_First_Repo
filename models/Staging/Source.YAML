version: 2
sources:
  - name: raw
    database: MY_FIRST_SNOWFLAKE_DATABASE
    schema: RAW_AREA

    tables:
      - name: company_dim
      - name: skills_dim
      - name: job_postings_fact
        config:
          freshness:
            warn_after:
              count: 24
              period: hour
          loaded_at_field: job_posted_date
      - name: skills_job_dim